/**
 * TOWER DEFENDER 2347
 * A 3D Space Tower Defense Shooter
 *
 * Entry point - initializes the game when the page loads
 */

import { initGame } from './game.js';

// Initialize game when page loads
window.addEventListener('load', initGame);
